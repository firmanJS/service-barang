## How To Use

```
run npm install 
run node ./bin/www
server runing in port 3001 ----> cek in folder bin/www
in browser http://localhost:3001/
test in postman get
set header x-token-api token get with login service
```
